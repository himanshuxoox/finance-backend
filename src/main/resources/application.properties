spring.application.name=finance-backend

# === PostgreSQL Config ===
spring.datasource.url=${DATASOURECE_URL}
spring.datasource.username=${DATASOURECE_USER}
spring.datasource.password=${DATASOURECE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# === CORS Support ===
#spring.web.cors.allowed-origins=${FRONTEND_URL}

jwt.secret=ZmluYW5jZS1hcHAtamF3dC1zZWNyZXQtbXlzaWduYXR1cmU=

logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG


frontend.url=${FRONTEND_URL}